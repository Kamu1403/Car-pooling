<view class="box">
  <view wx:for="{{test}}">

    <view class="item_name" data-value="{{item.dbname}}" bindtap="onclick">{{item.name}}</view>
  </view>

</view>

<mp-actionSheet bindactiontap="btnClick" show="{{showActionsheet}}" actions="{{groups}}" title="- 请选择操作 -">
</mp-actionSheet>